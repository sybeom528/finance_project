# 시계열 데이터 분석 학습 계획서

**작성일:** 2026-04-21
**목표:** Black-Litterman 프로젝트의 XGBoost 분류기를 시퀀스 모델(GRU)로 교체할 수 있는 수준의 시계열 분석 역량 확보
**예상 기간:** 4주 (주당 5~10시간)

---

## 학습의 큰 그림

현재 프로젝트는 다음 구조입니다.

```
입력: X_is.shape = (2310, 17)   ← 행마다 독립된 17개 피처
       ↓
모델: XGBoost                   ← 행 단위 학습 (날짜 순서 무시)
       ↓
출력: 5분위 확률 (proba[5])
       ↓
B-L: Q, Ω 추출 → 최적 포트폴리오
```

목표는 **모델만** 교체하는 것입니다.

```
입력: X_is.shape = (N, T, 17)    ← T일치 시퀀스를 묶어 입력
       ↓
모델: GRU/LSTM                   ← 시간 의존성 학습
       ↓
출력: 5분위 확률 (proba[5])      ← 동일
       ↓
B-L: Q, Ω 추출 → 최적 포트폴리오  ← 코드 수정 없음
```

**핵심:** 모델 입출력 인터페이스는 똑같이 유지하면서 내부만 바꾼다.

---

## 4주 커리큘럼 + 진행 체크리스트

### 1주차 — 시계열 기초 이론

> 목표: "왜 행 단위 모델이 아닌 시퀀스 모델이 필요한가"를 통계적 근거로 설명할 수 있게 됨

- [x] **1.1 시계열 데이터의 본질** ✅ 2026-04-21 ← `week1_시계열기초/01_시계열데이터의본질.md`
  - i.i.d. 가정 위반 — 행끼리 독립이 아니다
  - 왜 주가 원본 대신 수익률(log_return)을 쓰는가
- [x] **1.2 정상성 (Stationarity)** ✅ 2026-04-21 ← `week1_시계열기초/02_정상성.md` + `02_정상성_실습.ipynb`
  - 약정상성(weak stationarity)의 3가지 조건
  - ADF 검정 (Augmented Dickey-Fuller)
  - KPSS 검정 — ADF의 반대 가설
  - 차분(differencing)으로 정상성 확보
  - **부록 A**: 시계열 정규화 9가지 기법
  - **부록 B**: ADF vs KPSS 심화
- [ ] **1.3 자기상관 (Autocorrelation)** ← `week1_시계열기초/03_자기상관.md` + `03_자기상관_실습.ipynb`
  - ACF / PACF 시각화와 해석
  - Ljung-Box 검정 — 잔차 자기상관 진단
- [ ] **1.4 시계열 분해**
  - 추세 · 계절성 · 잔차
  - `seasonal_decompose`로 MSFT 수익률 직접 분해

**1주차 산출물:** `week1_시계열기초/실습_MSFT_정상성분석.ipynb`

---

### 2주차 — 시퀀스 데이터 구조 만들기

> 목표: 현재 `(2310, 17)` shape 데이터를 `(N, T, 17)` 텐서로 변환하는 파이프라인 직접 구현

- [ ] **2.1 Tabular vs Sequential 데이터 차이**
- [ ] **2.2 Sliding Window 기법**
  - lookback window(T)와 prediction horizon(h)
  - 윈도우 크기 결정 기준 (ACF 기반)
- [ ] **2.3 시계열 누수(Look-ahead Bias) 방지**
- [ ] **2.4 PyTorch Dataset / DataLoader 구현**
  - `torch.utils.data.Dataset` 상속
  - `__len__`, `__getitem__` 메서드
- [ ] **2.5 텐서 차원의 의미 (Batch, Time, Feature)**

**2주차 산출물:** `week2_시퀀스데이터/SequenceDataset.py`

---

### 3주차 — 시퀀스 모델 이해와 구현

> 목표: GRU가 무엇을 어떻게 학습하는지 설명할 수 있고, PyTorch로 학습 루프를 직접 짤 수 있게 됨

- [ ] **3.1 RNN의 기본 구조와 한계**
  - 은닉 상태(hidden state)의 의미
  - 기울기 소실(vanishing gradient) 문제
- [ ] **3.2 LSTM의 게이트 메커니즘**
  - Forget · Input · Output 게이트
  - 셀 상태(cell state)
- [ ] **3.3 GRU — 실무에서 더 많이 쓰는 이유**
- [ ] **3.4 PyTorch 학습 루프 표준 패턴**
- [ ] **3.5 분류 모델 출력 구성 — 5분위 확률**
- [ ] **3.6 학습 안정화 기법 (Dropout · Early Stopping · LR Scheduler)**

**3주차 산출물:** `week3_시퀀스모델/MSFT_GRU분류기.ipynb`

---

### 4주차 — 기존 파이프라인에 통합

> 목표: 현재 Walk-Forward + Black-Litterman 코드에서 모델만 GRU로 교체하고 XGBoost와 성능 비교

- [ ] **4.1 Walk-Forward 검증 + 시퀀스 모델**
- [ ] **4.2 다종목 학습 전략**
- [ ] **4.3 Black-Litterman 연결 검증**
- [ ] **4.4 성능 비교 (Sharpe · MDD · Hit Ratio)**
- [ ] **4.5 (선택) TCN, TFT 심화**

**4주차 산출물:** `week4_프로젝트통합/walkforward_GRU_BL.ipynb`

---

## 학습 진행 패턴

각 토픽마다 다음 4단계를 따릅니다.

```
1. 개념 설명 (.md 읽기)
        ↓
2. toy data로 동작 확인 (간단 예제)
        ↓
3. MSFT 단일 종목에 적용 (실제 데이터)
        ↓
4. 현재 프로젝트 구조에 어떻게 들어맞는지 검토
```

---

## 학습 메모 / 막힌 부분 기록

> 학습하면서 헷갈렸던 부분, 다시 봐야 할 부분을 여기에 적어둡니다.

(아직 비어있음 — 학습 진행하면서 채워나갑니다)

---

## 참고 자료

- statsmodels 공식 문서 — https://www.statsmodels.org/stable/tsa.html
- PyTorch Dataset/DataLoader 튜토리얼 — https://pytorch.org/tutorials/beginner/basics/data_tutorial.html
- Andrej Karpathy, "The Unreasonable Effectiveness of Recurrent Neural Networks" (2015)
- Hyndman & Athanasopoulos, *Forecasting: Principles and Practice* (3rd ed., 무료 온라인)
